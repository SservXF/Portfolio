import { BrowserRouter } from 'react-router-dom'
import Layout from './Layout'

function AppRoutes() {
  return (
    <BrowserRouter>
      <Layout />
    </BrowserRouter>
  )
}

export default AppRoutes